# Project 011 — Design and Evidence Contract

> **Current authority: Revision 8.** Revision 3's independent WebGL2 approximation is invalid for the user's requirement because it did not execute the researched library. Revision 4 corrected the runtime; Revision 5 organized the verified runtime and research into the canonical Web workspace. Revision 6 added bounded product-scenario demonstrations; Revision 7 turns the resulting understanding into a personal game/IP/asset growth map; Revision 8 packages the capability map and GitHub Pages delivery as one verifiable remote artifact.

## Contract record

- Entry mode: revision-led; replace source-first explanation with target-demo-effect reverse engineering
- Request revision: 2
- Target user: the owner of the research library and future collaborators deciding whether ABYSSAL should become an Environment Pack
- Desired first impression: the target visual result is explicit, and every technical explanation can be traced back to something the viewer should actually see
- Selected pattern: technical demo-film reverse engineering with a later bounded reproduction route
- Evidence branch: target demo media/runtime → observable effect decomposition → parameter and shot specification → source confirmation → Project 011 reproduction → bounded product decision
- Visual ambition: editorial technical brief; clarity and evidence hierarchy outrank spectacle in this planning revision
- Experience architecture: editorial flow with an anchored evidence route, not an application shell
- Primary journey: target frames → visible-result ladder → effect-to-system map → algorithm explanation → reproduction specification → phase gates → artifact re-entry
- User-defined phases: create the subproject; tailor the research plan; explain its meaning; define how it can land; revise the research so the actual target demonstration effect is the source of truth
- Autonomy authorization: the user explicitly corrected the research direction and authorized an in-scope revision; all Revision 2 changes are local and reversible
- User decision boundary: integrating upstream code, adding a root dependency or starting the GPU/POC phase remains a later evidence-backed decision

## Required inputs

- Upstream repository and fixed commit;
- official target-demo frames stored in that fixed commit;
- WebGL2-capable desktop browser and identified GPU for runtime work;
- Current research-library relationships, especially Projects 002, 008, 009 and 010;
- A future independent Three.js host for the POC;
- No upstream binary assets are required for the planning revision.

## Expected outputs

- Project 011 README and research plan;
- a target-effect ladder and effect-to-system reverse-engineering matrix;
- structured source audit and reuse manifest;
- POC scorecard template;
- public planning page and research-library registry entry;
- GitHub-readable capability map, remote URLs and reproducible Pages delivery record;
- later, only after the GPU gate, an independent Environment Pack POC.

## Visual constraints

- Use a single deep-ocean theme with cyan for verified runtime structure and amber for gates or unverified work.
- Lead with the target effect and the visual result ladder before the adoption judgment.
- The planning page must remain fully readable without JavaScript.
- Embed only attributed official target screenshots from the fixed commit; label them as `TARGET_DEMO_MEDIA`, not Project 011 runtime evidence.
- Use DOM for all research content; no decorative WebGL is needed during the planning revision.
- Keep motion optional and disabled by `prefers-reduced-motion`.

## Information constraints

The project must answer in this order:

1. What should the viewer see in the calm, cloud-field and violent-storm target states?
2. Which changes in scale, light, atmosphere, water, motion and camera make those states legible?
3. Which parameters, rendering systems and algorithms are required by each visible result?
4. Which claims are direct media observations, source confirmations, runtime measurements or product inferences?
5. What is the smallest reproduction that proves the result rather than merely exposing controls?
6. What would that reproduction mean to Projects 002, 008, 009 and 010?
7. Which gates, stop rules and evidence allow a bounded adoption decision?

Every significant statement must fit one label:

- `TARGET_DEMO_MEDIA`;
- `UPSTREAM_SOURCE`;
- `UPSTREAM_RUNTIME`;
- `PROJECT_011_POC`;
- `INFERENCE`.

## Operation constraints

- Section links use normal anchors and work without JavaScript.
- JavaScript may add reading progress and current-section state only.
- Artifact links must resolve in the built Pages output.
- External links must be limited to explanatory GitHub sources, not runtime dependencies.
- No copy, record, audio, viewer or upstream GPU action is presented as completed in Revision 2.
- The target screenshots must have explicit upstream attribution and must not be described as locally reproduced output.

## Environment constraints

- Static GitHub Pages-compatible output;
- no external fonts, trackers, scripts or styles;
- responsive at desktop, tablet and 390px phone widths;
- reduced-motion support;
- Page build copies the plan, audit, manifest and scorecard next to the public page.

## Revision 2 completion criteria

- Project 011 directory exists with no template placeholders;
- fixed upstream commit and license are recorded;
- current source evidence and missing runtime evidence are separated;
- research phases, gates, acceptance and stop rules are explicit;
- Environment Pack v0.1 hypothesis is described but not claimed as implemented;
- relationship to Projects 002, 008, 009 and 010 is concrete;
- public planning page, cover and project registry entry exist;
- targeted static checks and Pages build pass;
- existing unrelated dirty-worktree changes remain untouched.
- the target demonstration result appears before the source capability inventory;
- calm, cloud-field and violent-storm frames each have observable visual criteria;
- every primary visual criterion maps to parameters, a rendering system, an algorithmic principle and a reproduction test;
- the minimum POC is framed as a visual state transition and shot sequence, not a list of controls;
- affected desktop, tablet, phone, keyboard, no-script and reduced-motion evidence is rerun after the revision.

## Evidence coverage manifest

| Requirement | Artifact / evidence surface | Validation stage | Revision 2 status |
| --- | --- | --- | --- |
| Source freeze | README, source audit, reuse manifest | Stage 3 / content | pass |
| Target demo media provenance | fixed-commit media plus attribution | Stage 0 / goal lock | pass: three frames and hashes recorded |
| Visible-result ladder | reverse-engineering artifact and public page | Stage 3 / information | pass: calm → cloud field → violent storm → tsunami → aftermath |
| Effect-to-system map | observation → parameter → system → algorithm → test | Stage 3 / information | pass |
| Capability map | README and public page | Stage 3 / content | pass: reordered behind target effects |
| GPU baseline | Research plan Phase 1 | Stage 1 / environment | defer: live route returned `ERR_NETWORK_ACCESS_DENIED`; isolated `npm ci` was aborted; retest when network/dependency installation is available |
| Coupling map | Research plan Phase 2 | Stage 8 / responsive matrix | pass: source-level reverse map and future runtime gate defined |
| Environment contract | README hypothesis | Stage 2 / contract | pass: target beat and ShotSpec hypothesis updated; implementation is later phase work |
| POC | Research plan Phase 4 and scorecard | Stage 5 / interaction | pass: target-led scope and evidence scorecard defined; implementation is later phase work |
| Product decision | Three-state rule | Stage 3 / content | pass: decision criteria defined, decision not yet reached |
| Public route and artifact links | built Pages output | Stage 9 / static verification | pass |
| Desktop reading route | 1440 × 1000; `experiments/browser-validation.md` | Stage 7 / browser evidence | pass |
| Tablet reading route | 820 × 1180; `experiments/browser-validation.md` | Stage 7 / browser evidence | pass |
| Phone reading route | 390 × 844; `experiments/browser-validation.md` | Stage 7 / browser evidence | pass |
| Keyboard and focus | skip link, section anchors, artifact links | Stage 5 / interaction | pass |
| No-script readability | target media and reverse map remain understandable | Stage 8 / fallback | pass |
| Reduced motion | `prefers-reduced-motion: reduce` | Stage 7 / browser evidence | pass |
| Console and runtime errors | public route in Google Chrome | Stage 1 / environment | pass |

## Skill update boundary

Revision 2 must not update `webgl-product-film`. The skill may be updated only if Project 011 reproduces the target result in an independent POC and the conclusion generalizes beyond ABYSSAL.

---

## Revision 3 — independent target-effect POC

### Contract record

- Entry mode: revision-led implementation; the user explicitly said `继续` after confirming the target-effect-first route.
- Request revision: 3
- Current owning stage: Stage 0 goal lock, advancing directly to Stage 1 runnable baseline after this record.
- Visual ambition: immersive technical demo film.
- Experience architecture: Spatial Stage. The environment canvas remains the operating surface while beat navigation, playback, evidence labels and performance state remain in the foreground.
- Scene base: native WebGL2 fullscreen fragment-shader scene with a semantic DOM fallback.
- Scene persistence: visible throughout the five-beat journey; the research page remains a separate detail route.
- Foreground control model: play/pause, replay, previous/next beat, direct beat selection, timeline scrubber and reduced-motion-safe state feedback.
- State-to-scene mapping: `CALM`, `CLOUD FIELD`, `VIOLENT STORM`, `TSUNAMI`, `AFTERMATH`; unavailable WebGL2 maps to an attributed target-frame fallback and readable explanation.
- Mobile transformation: compact bottom control deck; the scene remains visible and direct beat selection remains reachable.
- Primary journey: open demo → understand the current beat → play the full 52-second escalation → jump to or scrub any beat → pause/replay → return to the research evidence.
- Autonomy authorization: the user's `继续` authorizes reversible implementation, runtime checks and documentation inside Project 011 without another phase confirmation.
- User-decision boundary: importing upstream code, shipping an upstream-derived production package, adding audio/voiceover, or claiming scientific disaster simulation remains outside this POC.

### Implementation boundary

- Do not copy upstream shaders or runtime source.
- Do not add a root runtime dependency for this bounded proof.
- Use the target media only as visual calibration evidence, never as locally rendered output.
- Render a coherent observable approximation, not a claim of FFT-ocean, volumetric-cloud or fluid-simulation equivalence.
- Keep every effect controlled by one timeline-derived director state; avoid scattered timers.
- Keep essential labels and controls operable when WebGL2 or animation is unavailable.

### Observable completion criteria

- A canonical local route renders an independent 52-second five-beat sequence.
- Each beat has a distinct sky, sea, atmosphere and camera-motion signature that can be identified without reading source.
- `TSUNAMI` produces an event-scale moving wall/crest and a lower chase-style framing rather than only increasing ordinary wave amplitude.
- Playback, pause, replay, previous/next, direct beat selection and scrubbing work in a real browser.
- The active beat, elapsed time, rendering path and measured frame state are visible to the user.
- Desktop, tablet and 390px phone layouts retain the scene and reachable controls without overflow.
- Keyboard focus is visible; reduced motion disables automatic cinematic camera motion while preserving beat inspection.
- The WebGL2-unavailable path preserves the explanation and controls as a readable fallback.
- The research page links to the runnable POC, and the README, scorecard, validation record and Pages build describe the same canonical route.

### Revision 3 coverage manifest

| User phase | Requirement / artifact | Surface or state | Evidence needed | Owning stage | Status | Next action |
| --- | --- | --- | --- | --- | --- | --- |
| Continue | Contract and independent implementation boundary | Design contract | inspectable Revision 3 record | Stage 0 | pass | establish runnable route |
| Continue | Five-beat spatial stage | desktop WebGL2 | browser screenshot plus active-beat observations | Stage 1–3 | pass | five fixed evidence frames inspected |
| Continue | Timeline and beat director | play, pause, replay, scrub, direct selection | browser interaction observations | Stage 4–6 | pass | all controls included in 48/48 browser run |
| Continue | Distinct target-effect signatures | all five beats | screenshots and computed runtime state | Stage 2 / 6 | pass, bounded approximation | calm, cloud, storm, tsunami and aftermath visually inspected |
| Continue | Responsive spatial stage | 1440px, 820px, 390px | screenshots, overflow and control reachability | Stage 7 | pass | no overflow; scene and controls remain visible |
| Continue | Keyboard and reduced motion | keyboard plus media preference | focus path and emulation evidence | Stage 7 | pass | visible focus and autoplay suppression verified |
| Continue | WebGL2 fallback | forced unavailable path | readable DOM fallback observation | Stage 8 | pass | `?renderer=dom` retains beat navigation and attribution |
| Continue | Performance observation | each beat on canonical browser path | FPS/frame-time sample with environment block | Stage 8 | pass as software smoke | SwiftShader 26.82–33.79 derived FPS; not a hardware claim |
| Continue | Named hardware GPU baseline | real Chrome / physical GPU | renderer identity plus per-beat frame time | Stage 8 | defer | retest when POC is run on a named hardware GPU |
| Continue | Research and build integration | public research route and Pages output | link checks and build output | Stage 9 | pass | runnable POC and two evidence artifacts integrated |
| Continue | Automated checks and delivery record | repository artifacts | targeted tests plus terminal audit | Stage 9 | pass | static, build and browser suites recorded |

### Skill update boundary

Revision 3 still does not automatically update `webgl-product-film`. First require an independently rendered sequence, real browser evidence, bounded performance measurements and a conclusion that distinguishes visual approximation from the upstream algorithms.

---

## Revision 4 — actual upstream runtime

### Corrected goal

The canonical demo must run the researched repository itself at fixed commit `849ff7f4199c9322d8ecafb48d62fc63f8d5af1d`. A look-alike, rewritten shader, screenshot montage or target-media fallback is not an implementation of the library.

### Runtime boundary

- Scene source: vendored upstream source at the fixed commit.
- Runtime: upstream `App`, Three.js, WebGL2, GLSL3, Director, Sandbox and post-processing.
- Project 011 responsibility: version lock, build, static publication, license retention, browser checks and evidence notes.
- Forbidden substitution: Project 011 must not replace upstream ocean, atmosphere, cloud, weather, event or post-processing shaders while claiming to show upstream output.
- Performance boundary: SwiftShader checks prove only functional execution; physical-GPU performance requires separately identified hardware evidence.

### Observable acceptance

- `window.__app.running` is true;
- WebGL2 canvas has a non-empty render buffer;
- upstream Director exposes all 11 authored acts;
- upstream Sandbox is installed;
- `director.gotoAct()` enters DEAD CALM, VIOLENT STORM, TSUNAMI and AFTERMATH;
- runtime state exposes upstream ocean/weather/camera diagnostics;
- no boot error, page exception or failed resource request occurs;
- the canonical route contains only the upstream production bundle plus attribution, not the Revision 3 shader.

### Evidence result

All 14 browser checks passed in Chrome using SwiftShader. The runtime is therefore valid as `UPSTREAM_RUNTIME`; real hardware performance, external-host decoupling and complete lifecycle disposal remain pending product gates.

---

## Revision 5 — Web understanding and demonstration workspace

### Design contract

- Entry mode: revision-led refinement of the existing Project 011 research page.
- Request revision: 5.
- Target user and context: the research-library owner deciding what `natural-disasters` actually is, what can be demonstrated now, and where extension is justified.
- Desired first impression: this is an ocean/extreme-weather runtime with many states and events, not a generic all-disaster engine; the visible stage is the actual fixed upstream build.
- Visual ambition: immersive editorial research workspace.
- Experience architecture: Hybrid Workspace.
- Scene base: same-origin iframe containing the actual upstream WebGL build at the fixed commit.
- Scene persistence: visible throughout the primary demonstration operation; deeper capability and decision material follows in semantic document flow.
- Foreground control model: four evidence-act controls, full Director reset, Sandbox handoff and full-screen launch.
- State-to-scene mapping: loading, ready, selected act, Sandbox handoff, runtime error and external-launch fallback.
- Mobile transformation: no forced inline WebGL boot below 720 px; retain act explanations and explicit full-demo links to avoid hiding the research behind a heavy canvas.
- Fallback: all understanding, capability, use-case, extension and decision content remains readable without JavaScript or WebGL.
- Visual constraints: deep-ocean neutral palette; cyan means verified runtime, amber means extension gate; no decorative replacement canvas or fabricated disaster imagery.
- Information constraints: order is boundary → actual runtime → capability stack → current use cases → extension ladder → value to us → evidence.
- Operation constraints: act controls must invoke upstream `director.gotoAct()` in the iframe when ready; no Project 011-authored renderer; keyboard focus remains visible.
- State constraints: selected act label and explanation must agree with the actual act index; runtime failure must expose a recovery link.
- Environment constraints: canonical route `http://127.0.0.1:4173/projects/natural-disasters-environment-study/`; same-origin demo route; desktop 1440×1000, tablet 820×1180 and phone 390×844; dark theme only; reduced-motion support.
- Primary journey: understand the boundary → run four upstream evidence acts → inspect Sandbox/full demo → compare extension cost → read the bounded landing recommendation.
- Required artifacts: revised page HTML/CSS/JS, static validation, browser validation record, updated README entry and Pages build.
- Autonomy authorization: the user explicitly requested Web implementation; reversible work inside Project 011 is authorized.
- User-decision boundary: adding new upstream shaders/events, GLB assets, backend persistence, scientific simulation or publishing video remains outside this revision.

### Observable completion criteria

- The first viewport states the product boundary and presents the actual upstream stage.
- The embedded stage loads the fixed upstream runtime on desktop and exposes runtime-ready status.
- Calm, violent storm, tsunami and aftermath controls call the upstream Director and update the foreground explanation.
- Sandbox and full-demo paths remain available without duplicating upstream controls.
- Capability, use-case and extension sections distinguish current ability, bounded adaptation and major rewrite.
- Desktop, tablet and phone retain the primary understanding journey without horizontal overflow.
- Keyboard focus and reduced-motion behavior are visible and usable.
- Static checks, browser checks and Pages build pass.

### Coverage manifest

| User phase | Requirement | Surface / state | Evidence | Stage | Status | Next action |
| --- | --- | --- | --- | --- | --- | --- |
| Web 梳理 | Boundary and first-view hierarchy | desktop default | screenshot + DOM | 2–3 | pass | first view states the runtime boundary and places the live evidence stage before analysis |
| Web 梳理 | Actual upstream embedded runtime | desktop ready | iframe runtime observation | 1 / 5 | pass | observed upstream `window.__app.running`, 11 Director acts and WebGL canvas |
| Web 梳理 | Four evidence-act controls | calm/storm/tsunami/aftermath | interaction observations | 4–6 | pass | all four controls invoked the corresponding upstream Director acts |
| Web 梳理 | Sandbox and full demo | desktop operation | link/control evidence | 4–5 | pass | Sandbox activation and independent canonical demo handoff both verified |
| Web 梳理 | Capability and extension map | semantic document | DOM/content inspection | 3 | pass | boundary, six-layer stack, three scenario rings and five-level ladder are present |
| Web 梳理 | Responsive transformation | 820px / 390px | screenshots + overflow checks | 7 | pass | desktop, tablet and phone passed without horizontal overflow; phone uses the explicit lightweight handoff |
| Web 梳理 | Keyboard and reduced motion | desktop input / media setting | browser observation | 7 | pass | real Tab-path focus visibility and reduced-motion behavior verified |
| Web 梳理 | Runtime fallback | forced non-embed / script-disabled meaning | DOM observation | 8 | pass | `embed=0` retains the research document and provides the direct upstream launch path |
| Web 梳理 | Engineering delivery | static checks + Pages build | terminal output | 9 | pass | 23 static checks, 30 browser checks and Pages build passed |

Revision 5 terminal audit: every manifest row is closed. Browser automation used ANGLE SwiftShader with the `potato` preset, so these checks establish functional integration and interaction correctness—not physical-GPU performance.

---

## Revision 6 — Extension scenario demonstrations

### Design contract

- Entry mode: revision-led expansion of the existing Hybrid Workspace.
- Direct authorization: the user asked to build several extension-scene demonstrations.
- Selected pattern: product-scenario prototype over the verified upstream runtime.
- Evidence branch: upstream `Sandbox` conditions and event methods, plus Project 011's explicitly labelled DOM business layer.
- Required inputs: the fixed upstream build already vendored at commit `849ff7f4199c9322d8ecafb48d62fc63f8d5af1d`; no new external assets or services.
- Expected output: three selectable demonstrations—port typhoon warning, near-shore tsunami evacuation and offshore-platform storm operations.
- Visual ambition: Immersive.
- Experience architecture: Hybrid Workspace. The live ocean stage remains the visual anchor; scenario selection and explanation stay beside it; the research document remains below.
- Scene base: the actual upstream WebGL iframe. No replacement renderer, copied shader or fabricated disaster canvas.
- Foreground control model: scenario selector, live status, business overlay and one-click return to the upstream evidence acts.
- State-to-scene mapping: each scenario must change both the actual upstream environment/event state and the labelled Project 011 business overlay.
- Mobile transformation: retain the readable scenario cards and explicit launch path while the expensive inline WebGL remains disabled below 720 px.
- Fallback: scenario meaning and boundaries stay readable when embedding is forced off or unavailable.
- Boundary: port assets, risk bands, timers and checklists are interface demonstrations, not geospatial, inundation, structural or prediction outputs.
- Primary journey: load the real runtime → select each extension scenario → observe actual upstream environmental response plus a business layer → return to the original evidence acts.
- Observable completion criteria: all three scenarios call upstream runtime methods, display distinct business overlays and status copy, reset cleanly, work at desktop/tablet/phone boundaries, preserve keyboard focus/reduced motion/fallback, pass static checks, browser checks and Pages build.

### Coverage manifest

| User phase | Requirement | Surface / state | Evidence | Stage | Status | Next action |
| --- | --- | --- | --- | --- | --- | --- |
| 扩展场景 | Preserve actual upstream runtime boundary | desktop live stage | iframe runtime observation | 1 / 5 | pass | fixed upstream App remained running; scenario methods were observed on its Sandbox and Director objects |
| 扩展场景 | Port typhoon warning demonstration | port scenario active | interaction + screenshot | 4–6 | pass | upstream hurricane object, storm target and labelled port overlay observed |
| 扩展场景 | Near-shore tsunami evacuation demonstration | tsunami scenario active | interaction + screenshot | 4–6 | pass | one upstream soliton and the labelled three-band evacuation overlay observed |
| 扩展场景 | Offshore-platform storm-operations demonstration | platform scenario active | interaction + screenshot | 4–6 | pass | upstream night target, eight lightning triggers and labelled operations overlay observed |
| 扩展场景 | Explicit authored/upstream boundary | every scenario | DOM/content inspection | 3 / 6 | pass | every overlay says `PROJECT 011 / BUSINESS LAYER` and carries a scenario-specific non-scientific boundary |
| 扩展场景 | Return to original evidence acts | scenario → act | interaction observation | 5 / 6 | pass | return cleared all overlays, exited Sandbox and restored DEAD CALM |
| 扩展场景 | Responsive and fallback behavior | 820 px / 390 px / embed=0 | screenshots + DOM | 7–8 | pass | tablet and phone had no overflow; phone and forced fallback retained readable scenario meaning and real-demo launch |
| 扩展场景 | Keyboard and reduced motion | desktop input/media | browser observation | 7 | pass | scenario controls were focus-visible and the reduced-motion stylesheet disabled the radar animation duration |
| 扩展场景 | Engineering delivery | static/browser checks + Pages build | terminal output | 9 | pass | 36 browser checks and Pages build passed; final static suite recorded at delivery |

Revision 6 terminal audit: every extension row is closed. The final screenshots show three distinct product layers over the same upstream ocean environment. ANGLE SwiftShader remains functional evidence only; physical-GPU performance and scientific accuracy are not claimed.

---

## Revision 7 — Personal extension map

### Design contract

- Entry mode: revision-led information expansion of the existing Web workspace.
- Direct authorization: the user asked to add our personal-extension understanding to the Web description.
- Selected pattern: story-driven personal extension map inside the existing Hybrid Workspace.
- Evidence branch: fixed upstream capability map plus the three Revision 6 scenario proofs.
- Required inputs: no new runtime, asset or external service.
- Expected output: one visible section connecting a shared environment core to game, personal IP and reusable material outputs, followed by an explicit execution order.
- Target reader: the user evaluating how this research compounds into personal work rather than an enterprise-only case.
- Desired first impression: the library is not one demo; it can become a reusable personal content engine when paired with gameplay, identity and asset production.
- Information constraints: clearly separate what the library already provides from what must be added; do not imply that characters, gameplay systems, GLB assets or publishing packages already exist.
- Visual constraints: reuse the current deep-ocean editorial system; present three directions as one connected flywheel rather than another generic card list.
- Primary journey: common use cases → personal growth flywheel → recommended four-step sequence → technical extension cost.
- Responsive boundary: the three-track map must remain legible at 1440, 820 and 390 px without horizontal overflow.
- Observable completion criteria: GAME, PERSONAL IP and ASSET LIBRARY are all visible; each names concrete outputs and missing inputs; the shared environment core and four-step recommendation are explicit; navigation reaches the section; static checks, browser layout checks and Pages build pass.

### Coverage manifest

| User phase | Requirement | Surface / state | Evidence | Stage | Status | Next action |
| --- | --- | --- | --- | --- | --- | --- |
| Web 描述 | Personal meaning follows common use cases | document flow | DOM order + screenshot | 3 | pass | personal map appears between common use cases and the technical extension ladder |
| Web 描述 | Game direction is concrete and bounded | personal map / game | DOM/content inspection | 3 | pass | playable slice and three outputs are paired with missing player, vessel, collision, mission and persistence systems |
| Web 描述 | Personal IP direction is concrete and bounded | personal map / IP | DOM/content inspection | 3 | pass | identity/world/content outputs are paired with missing character, symbol, world and publishing cadence inputs |
| Web 描述 | Material direction is concrete and bounded | personal map / assets | DOM/content inspection | 3 | pass | presets/shots/UI/media outputs are paired with metadata, version, format, budget and license needs |
| Web 描述 | One compounding route connects all three | flywheel + sequence | screenshot + DOM | 2–3 | pass | shared environment core and the four-step slice → identity → assets → kit order are visible |
| Web 描述 | Responsive reading flow | 1440 / 820 / 390 | screenshots + overflow checks | 7 | pass | desktop four-column map, tablet two-column tracks and phone single-column flow passed without horizontal overflow |
| Web 描述 | Existing runtime journey is preserved | scenario and evidence controls | browser interaction | 5 / 7 | pass | all prior runtime, scenario, reset, keyboard, reduced-motion and fallback checks remained green |
| Web 描述 | Engineering delivery | static/browser checks + Pages build | terminal output | 9 | pass | 39 browser checks and Pages build passed; final static suite recorded at delivery |

Revision 7 terminal audit: all personal-map rows are closed. The route is derived from demonstrated environment capabilities and explicitly names the gameplay, identity and asset-production work that does not yet exist.

---

## Revision 8 — GitHub remote delivery

### Design contract

- Entry mode: production hardening and remote publication of the verified Project 011 workspace.
- Direct authorization: the user asked to organize the current library capability, deploy the related Web to remote GitHub and submit the related information.
- Selected pattern: one versioned delivery containing the actual upstream runtime, capability map, Web explanation, extension demonstrations, personal routes and evidence.
- Source boundary: upstream runtime remains pinned to `849ff7f4199c9322d8ecafb48d62fc63f8d5af1d`; Project 011 adds packaging, research, business overlays and verification only.
- Deployment target: `main` on `yydshly/0828_codex_project`, published by the existing GitHub Pages workflow.
- Safety boundary: construct the delivery from the latest remote `main` in an isolated worktree so unrelated local staged and unstaged changes are not included.
- Observable completion criteria: Project 011 is readable on GitHub; the Pages workflow succeeds; the public research route and actual runtime route return successfully; capability, extension, personal and boundary content is present in the deployed HTML; local static, build and browser checks remain green.

### Coverage manifest

| Requirement | Evidence | Status |
| --- | --- | --- |
| GitHub-readable capability organization | `CAPABILITIES.md` plus Project 011 README | pass |
| Web delivery information | `08 / GITHUB DELIVERY` section and canonical remote URLs | pass |
| Reproducible publication | `.github/workflows/pages.yml` and `scripts/build-pages.mjs` | pending remote run |
| Scope isolation | clean worktree based on latest `origin/main` | pending commit audit |
| Functional integrity | static, Pages build and browser verification | pending final preflight |
| Remote availability | GitHub source, Pages research route and actual runtime route | pending deployment |
