# Project 011 — Web Workbench Validation

- Date: 2026-08-29
- Revision: 7
- Canonical URL: `http://127.0.0.1:4173/projects/natural-disasters-environment-study/`
- Automated URL: `?embedPreset=potato`
- Browser: Google Chrome controlled by Playwright 1.57.0
- Embedded renderer: ANGLE SwiftShader functional smoke
- Result: **39/39 pass**

The preferred `agent-browser` CLI was not installed on this host. The bundled Playwright/Chrome runtime provided the same required browser evidence, so this was a validation-route fallback rather than a product blocker.

## Primary journey

| Check group | Result |
| --- | --- |
| HTTP route, first-view boundary and `UPSTREAM_RUNTIME` label | pass |
| Same-origin iframe starts actual upstream `App` | pass |
| WebGL2 canvas render buffer exists | pass |
| Upstream Director exposes 11 acts | pass |
| Upstream Sandbox is installed | pass |
| DEAD CALM, VIOLENT STORM, TSUNAMI and AFTERMATH controls call upstream `gotoAct()` | pass |
| Port warning creates an upstream hurricane and the labelled port business layer | pass |
| Coastal evacuation creates an upstream tsunami soliton and the labelled evacuation layer | pass |
| Platform operations applies upstream night weather, triggers eight lightning bolts and shows the labelled operations layer | pass |
| Scenario return clears the business layer, exits Sandbox and returns to DEAD CALM | pass |
| Personal map contains GAME, PERSONAL IP and ASSET LIBRARY with three output blocks and three explicit gaps | pass |
| Personal map connects the shared environment core to one four-step execution order | pass |
| Sandbox handoff calls upstream `sandbox.setActive(true)` | pass |
| Director reset exits Sandbox and returns to DEAD CALM | pass |
| Six capability layers, three use-case boundaries and five extension levels | pass |
| Desktop, tablet and phone horizontal overflow | pass |
| Keyboard focus and reduced-motion behavior | pass |
| Phone lightweight mode retains full content and real-demo link | pass |
| Forced `embed=0` fallback remains readable and operable | pass |
| Page exceptions, console errors and failed requests | zero |

## Viewports

| Surface | Viewport | Runtime behavior | Result |
| --- | --- | --- | --- |
| Desktop | 1440 × 1000 | embedded upstream WebGL enabled | pass |
| Tablet | 820 × 1180 | embedded upstream stage retained | pass |
| Phone | 390 × 844 | inline WebGL disabled; semantic content, personal map and full-demo launch retained | pass |

## Evidence captures

Transient final captures are stored outside the repository:

```text
C:/Users/yun68/AppData/Local/Temp/project-011-web-workbench-evidence-20260829/
  desktop-default-workbench.png
  desktop-tsunami-workbench.png
  desktop-personal-map.png
  extension-port.png
  extension-tsunami.png
  extension-platform.png
  tablet-workbench.png
  phone-workbench.png
```

## Boundary

This validates the Web understanding/demonstration workspace, all three bounded extension scenes, the personal game/IP/asset map and the functional connection to the fixed upstream runtime. The disaster pixels are rendered by upstream; the labelled port, evacuation and operations layers are Project 011-authored product-interface demonstrations. The personal map is a bounded product route, not evidence that gameplay, character assets or a publishing package already exist. SwiftShader frame time is not physical-GPU performance evidence. Scientific accuracy, multiple runtime instances and complete upstream `App.dispose()` remain outside Revision 7.
